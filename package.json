{
  "name": "@mono-repo/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev" : "nx run-many --target=start:dev",
    "dev:start" : "nx run-many --target=start:dev --output-style=stream",
    "cleanAll" : "nx reset && nx run-many --target=clean --output-style=stram && rm -rf node_nodules && rm -rf apps/*/node_nodules"
    
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nx/js": "21.2.2",
    "@swc-node/register": "~1.9.1",
    "@swc/core": "~1.5.7",
    "@swc/helpers": "~0.5.11",
    "nx": "21.2.2",
    "prettier": "^2.6.2",
    "tslib": "^2.3.0",
    "typescript": "~5.8.2"
  }
}
